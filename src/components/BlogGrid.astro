---

---

<div class="grid-content">
  <slot />
</div>

<style>
  .grid-content {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-column-gap: 1em;
    background: #000;
  }
</style>
